# Week 9 — Introduzione a Python: Variabili, Tipi di Dato, Collections, Logica e Loops

Questa settimana è stata la vera entrata nel mondo Python. Algoritmi, strutture dati, logica, cicli: il pacchetto base per iniziare a programmare con criterio.

## Competenze sviluppate

- Gestito variabili e data types (`int`, `float`, `str`, `bool`, `None`)
- Usate funzioni built-in: `print()`, `type()`, `len()`, `help()`
- Manipolate liste con indexing, slicing e metodi (`append`, `extend`, `sort`)
- Compreso funzionamento di tuple, set, dizionari e relativi metodi
- Applicati costrutti logici `if`, `elif`, `else` con operatori di confronto
- Lavorato con cicli `while` e `for`, evitando loop infiniti
- Controllato il flusso con `break` e `continue`
- Usate funzioni per i loop: `range`, `enumerate`, `zip`
- Costruiti algoritmi chiari e step-by-step

## Sfide risolte

- Scrittura di algoritmi senza confusione
- Differenza netta tra indexing e slicing
- Gestione corretta delle condizioni nei loop
- Organizzazione precisa con `if` / `elif` / `else`
- Padronanza di `range()`, `enumerate()`, `zip()`
- Gestione ordinata di dizionari e set senza errori
- Controllo consapevole del flusso invece di codice "a caso"

## Esercizi completati

### W9D1 – Teoria + Pratica

- Algoritmi in forma testuale
- Variabili, assegnazione, tipi di dato
- `print()`, `type()`, operatori matematici
- Manipolazione stringhe: `upper`, `lower`, `replace`, `split`, `strip`
- Calcolo di tempi e valori numerici
- Liste: indexing, slicing, metodi
- Inserimento dati mancanti con `.append()`
- Esercizi guidati per fissare sintassi e logica

### W9D2 – Teoria

- Tuple: immutabilità, indexing, slicing, metodi
- Set: unicità, metodi insiemistici
- Dizionari: chiavi-valori, metodi base
- Commenti mono e multi-linea
- Costrutti logici e casi reali
- Loop `while` e controllo delle condizioni
- Loop `for` per attraversare collezioni
- `input()` e type casting

### W9D2 – Extra

- `range()` per iterazioni numeriche
- `enumerate()` per contatori interni
- `zip()` per iterare su più liste
- `break` e `continue` in casi pratici
- Ricerca e filtraggio dati durante i loop

## Highlight

Python inizia a diventare naturale. Questa settimana ha chiarito: come ragionare per algoritmi, come strutturare i dati, come far girare il flusso in modo controllato, come evitare errori logici nei cicli, 
e come manipolare collezioni reali. È la base che userò in tutto quello che viene dopo.

---

# Week 9 — Introduction to Python: Variables, Data Types, Collections, Logic, and Loops

This week marked my real entry into Python. Algorithms, data structures, logic, loops: the core toolkit to start programming with intention.

## Skills developed

- Managed variables and data types (`int`, `float`, `str`, `bool`, `None`)
- Used built-in functions: `print()`, `type()`, `len()`, `help()`
- Manipulated lists using indexing, slicing, and methods (`append`, `extend`, `sort`)
- Understood how tuples, sets, and dictionaries work and how to use their main methods
- Applied logical structures: `if`, `elif`, `else` with comparison operators
- Worked with `while` and `for` loops, avoiding infinite loops
- Controlled program flow using `break` and `continue`
- Used loop utilities: `range`, `enumerate`, `zip`
- Built clear, step-by-step algorithms

## Challenges overcome

- Wrote algorithms without confusion
- Understood the difference between indexing and slicing
- Managed loop conditions correctly
- Structured logic with `if` / `elif` / `else` cleanly
- Gained confident use of `range()`, `enumerate()`, and `zip()`
- Handled dictionaries and sets without errors
- Maintained conscious control of code flow instead of writing "random" logic

## Completed exercises

### W9D1 – Theory + Practice

- Algorithms written in textual form
- Variables, assignment, data types
- `print()`, `type()`, mathematical operators
- String operations: `upper`, `lower`, `replace`, `split`, `strip`
- Practical numeric calculations
- Lists: indexing, slicing, methods
- Filling missing data using `.append()`
- Guided exercises to reinforce syntax and logic

### W9D2 – Theory

- Tuples: immutability, indexing, slicing, methods
- Sets: uniqueness and set-based operators
- Dictionaries: key-value structure and main methods
- Single-line and multi-line comments
- Logical structures and applied examples
- `while` loops and condition control
- `for` loops for iterating over collections
- `input()` and type casting

### W9D2 – Extra

- `range()` for numerical iterations
- `enumerate()` for counters inside loops
- `zip()` for parallel iteration over multiple lists
- `break` and `continue` in real cases
- Searching and filtering data during loops

## Highlight

Python is starting to feel natural. This week clarified: how to think in terms of algorithms, how to structure data, how to control program flow, how to avoid logical errors in loops, 
and how to work with real collections. This foundation is what I'll use for everything that comes next.

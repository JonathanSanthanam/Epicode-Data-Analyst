Week 9 — Introduzione a Python: Variabili, Tipi di Dato, Collections, Logica e Loops

Questa settimana è stata la vera entrata nel mondo Python.
Algoritmi, strutture dati, logica, cicli: il pacchetto base per iniziare a programmare con criterio.

Competenze sviluppate

Gestito variabili e data types (int, float, str, bool, None).

Usate funzioni built-in: print(), type(), len(), help().

Manipolate liste con indexing, slicing e metodi (append, extend, sort).

Compreso funzionamento di tuple, set, dizionari e relativi metodi.

Applicati costrutti logici if, elif, else con operatori di confronto.

Lavorato con cicli while e for, evitando loop infiniti.

Controllato il flusso con break e continue.

Usate funzioni per i loop: range, enumerate, zip.

Costruiti algoritmi chiari e step-by-step.

Sfide risolte

Scrittura di algoritmi senza confusione.

Differenza netta tra indexing e slicing.

Gestione corretta delle condizioni nei loop.

Organizzazione precisa con if / elif / else.

Padronanza di range(), enumerate(), zip().

Gestione ordinata di dizionari e set senza errori.

Controllo consapevole del flusso invece di codice “a caso”.

Esercizi completati
W9D1 – Teoria + Pratica

Algoritmi in forma testuale.

Variabili, assegnazione, tipi di dato.

print(), type(), operatori matematici.

Manipolazione stringhe: upper, lower, replace, split, strip.

Calcolo di tempi e valori numerici.

Liste: indexing, slicing, metodi.

Inserimento dati mancanti con .append().

Esercizi guidati per fissare sintassi e logica.

W9D2 – Teoria

Tuple: immutabilità, indexing, slicing, metodi.

Set: unicità, metodi insiemistici.

Dizionari: chiavi-valori, metodi base.

Commenti mono e multi-linea.

Costrutti logici e casi reali.

Loop while e controllo delle condizioni.

Loop for per attraversare collezioni.

input() e type casting.

W9D2 – Extra

range() per iterazioni numeriche.

enumerate() per contatori interni.

zip() per iterare su più liste.

break e continue in casi pratici.

Ricerca e filtraggio dati durante i loop.

Highlight

Python inizia a diventare naturale.
Questa settimana ha chiarito:

come ragionare per algoritmi

come strutturare i dati

come far girare il flusso in modo controllato

come evitare errori logici nei cicli

come manipolare collezioni reali

È la base che userò in tutto quello che viene dopo.
